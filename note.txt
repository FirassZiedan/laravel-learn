$ php artisan tinker ;

>>> App\Task::all();
// Get all the stored data

>>> App\Task::where('id','>',2)->get();
// Add where statment to the query

>>> App\Task::pluck('task');
// Get a spcefic field

>>> App\Task::pluck('task')->first();
// Get first Record

>>> App\Task::find(1); || App\Task::first();
//Get the first Record


$ php artisan make:model Task -m
// Create task model and migrate file once


>>> $task = new App\Task ;
//Create new object to insert new data to database

>>> $task->task = "Task number One";
//Insert data to task field

>>> $task->save();
// TO Save all data into database

>>>App\Task::first()->task;
// Get data from spacific field in object

>>>App\Task::where('completed',0);
// Get data where match the condition

>>> App\Task::incomplete()->get();
// Call funtion from modle

layout.blade.php
<body>
  @yield(content);
</body>

*.blade.php
@extends ('layout');

@section ('content')
 ...
@endsection
//create layout and set where the content should be apper


//After Clone laravel Project you must 
$ composer install;
* copy .env.example to .env
$ php artisan key:generate;
$ php artisan migrate

// Work With scss and js
//you need to work with webpack.mix.js 
//it's faster and cleaner to work 
//also it's complie and compress the files
$ npm  install // just in first time 

//run npm run , Can read pachege.json
$npm run dev
Or
$npm run watch 